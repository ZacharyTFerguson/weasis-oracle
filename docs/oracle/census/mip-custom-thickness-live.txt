# Live 2D MipMenu Custom thickness (AppLauncher 4.7.0, DISPLAY=:1)

`MipMenu.showCustomThicknessDialog`. Opened from MIP thickness → Custom thickness after selecting Max (`212-mip-custom-thickness.png`).

JDialog title: Custom thickness (332×136)
Label: Image Extension:
JSpinner SpinnerNumberModel value=2 (current MIP_THICKNESS; range 1..max(series.size/2, 1) = 3 on this 6-slice stack)
Conversion label:  = 55 mm   ((value * 2 + 1) * sliceThickness)
Buttons: OK, Cancel

Closed with Cancel (does not call updateSize). Overlay on the MipView after Max: Thickness 41 mm / Location 20 mm.

Do not tick G-P0-014.
