# G-P0-014 method coverage

Second-person whole-census **Sign**: [`g-p0-019-reviewer.md`](g-p0-019-reviewer.md) at `82cc891` (signer `bc-f5d7dd6f-4373-5f7a-8b78-72c90d7188a6`). Checklist `G-P0-014` / `G-P0-019` ticked from that file. `G-P0-015` stays open. 2D Viewer flyouts including Filter / Zoom / Orientation / Cine / Sort Stack / Reset / Open in new tab are captured (`143`–`149`). Nested image-context flyouts are captured (`164`–`170`).

| Method item | Evidence |
| --- | --- |
| Enumerate shipped `bundle/` | `docs/oracle/census/bundle-inventory.json` (48 + SHA-256). Live Linux amd64 GUI `felix:lb`: [felix-lb-live.txt](felix-lb-live.txt) (38 rows including System Bundle). 8 other-arch natives not started. Acquire editor/explorer + `weasis-base-explorer` are Dicomizer / non-DICOM explorer configs, not default `base.json`. |
| Open one object per claimed TS | `binary-run.md` Per-TS table; unique-UID GUI `150`–`151`, `156`–`161`, `163`; JPEG-XL hashed `.110` `162`; Implicit VR LE `224`; Explicit VR BE `225`; MPEG-2/4 is **`video/dicom` + Default System Application**, not a 2D still ([mpeg-video.md](mpeg-video.md), explorer `222`/`223`, OPEN dump [mpeg-open-h014.txt](mpeg-open-h014.txt) + titled handler `234-mpeg-xdg-open.png`); HTJ2K `13`/`56`/`57` + library `G-P0-016`. JPEG-LS `.81` is export-combo only (no open phantom). Encapsulated `encap/dicom` OPEN `236` + [encap-open-r014.txt](encap-open-r014.txt). DICOM AU player `235` ([au-encap.md](au-encap.md)). |
| Screenshot every menu | File/View/2D Viewer/Help plus submenus 136–142; 2D flyouts LUT/LUT Shape/Preset plus Filter/Zoom/Orientation/Cine/Sort/Reset/Open in new tab `143`–`149`. Live Sort Stack dump includes **Acquisition Time** ([2dviewer-sortstack-live.txt](2dviewer-sortstack-live.txt)); live Preset dump includes **Lung** ([2dviewer-preset-live.txt](2dviewer-preset-live.txt)). Oblique MPR menubar `221-oblique-mpr-menu.png` (Preset through Reset; no Cine / Sort Stack / Open in new tab). Dump: [oblique-mpr-menu.txt](oblique-mpr-menu.txt). |
| Context menu | Root `12-context-menu.png` / desktop recapture `164-context-menu.png`. Nested flyouts `165`–`170`. Explorer thumbnail live `182` + 2D Viewer flyout `183`. Viewer tab popup live `184` (Close Others / Close All / Maximize / Close). [explorer-context-menu.md](explorer-context-menu.md). Context `165`/`166` cropped Acquisition Time / Lung; those items are in the 2D Viewer dumps above. |
| Toolbar | Toggle list `136-view-toolbars.png`. Extra bars enabled: `172-toolbars-all-on.png` (cine skip/play/stop + KO star actions on the main chrome). Live flyouts: measure `191`, draw `192`, layout `193` (includes DICOM dump + Image Histogram), synch `194`, zoom type `195`, mouse action `197`. DICOM Information frame `189`–`190`. Layout views: Image Histogram `200`, DICOM dump pane `201`, Histogram Statistics dialog `207`. Oblique MPR 3-pane `202` (≥5 images) + MPR-context MIP popup `203`. 2D `MipMenu` `204`–`206` plus Custom thickness dialog `212` (Image Extension spinner, mm conversion, OK/Cancel). MPR-context nested MIP thickness `213` (Reset thickness + Custom thickness; mm from `minPixelRatio`), MPR Custom thickness `214` (`N pix = mm`), All views `215`, MIP type None/Min/Mean/Max `216`, Change MPR preferences `217`. MPR Synchronize `218` (Series Scroll through Spatial Unit, Apply to all views, Close). 3D toolbar click `208` (Opengl Error, no volume tab). 3D chrome that exists when OpenGL works is in [viewer3d-bytecode.md](viewer3d-bytecode.md) (`ActionVol` + [viewer3d-messages.properties](viewer3d-messages.properties)). KO Selection combo `209` (None / [11]; disabled while `MipView` sets `no.ko`). RT Tool (FOR-linked RTSTRUCT): east-strip tab `237`, normalized panel `238`, Load RT **PHANTOM / BODY** `239` ([rt-tool.md](rt-tool.md)). [toolbar-flyouts.md](toolbar-flyouts.md). |
| Prefs pages | `28`–`43`, `47` Language |
| Console `help` | Default GUI does not bind 17179. Live transcript with shipped `base-shell.json`: `gogo-help-live.txt` |
| Min OS / file associations | `min-os.md`, `windows-installer.md`, `windows-msi-launchcondition.txt`, `github-v4.7.0-assets.md`, `debian-control-arm64.txt`, `file-associations.properties`, `Weasis.desktop`. Windows source WiX undeclared; shipped x64 MSI `VersionNT >= 600` (Vista+, not Win10). Linux arm64 `.deb` same Depends as amd64, no Ubuntu floor. |
| `weasis.properties` keys | Shipped 114: `docs/oracle/weasis-properties.json`. Runtime overlay after Felix boot: [weasis-properties-runtime.txt](weasis-properties-runtime.txt) (SHA-256 `580e6cde…`) plus gogo `cat` / `weasis:info -a` ([gogo-weasis-info.txt](gogo-weasis-info.txt)). GUI: `171-about-sysinfo-live.png`. |
| Launcher args | `Weasis.cfg` |
| Update check | First-run Yes/No pane `01-main.png` (`WeasisWin.checkReleaseUpdate`, keys `weasis.update.release` + `weasis.show.update.next.release`). Live Help flyout `210-help-menu.png` ([help-menu-live.txt](help-menu-live.txt)): string is **Check for Updates...**. Click opens Chrome to `https://weasis.org/en/getting-started/download-dicom-viewer/` (`211-updates-after-click.png`) because frozen 4.7.0 < `nroduit.github.io/en/api/release/api.json` `v4.7.3` (2026-08-25). Overlay `weasis.update.release=false` skips only the first-run Yes/No pane, not this Help path (`WeasisWin` Help item calls `getLastRelease()` then `openBrowser`). The Update `JOptionPane` is `APPLICATION_MODAL`; on this VM its X11 peer was often zero (“Window must not be zero”). |
| Log viewer | `131-log-folder.png` |
| About / help | `60-about.png`, `61-about-system-information.png`, Xvfb Help `45-help-menu.png`, desktop Help recapture `210-help-menu.png` (live item **Check for Updates...**). Live System Information `171-about-sysinfo-live.png`. Help → Shortcuts: generated HTML [weasis-shortcuts.html](weasis-shortcuts.html) + `173-help-shortcuts.png`. Online Help opens `https://weasis.org/en/tutorials/` (`219-online-help.png`). Submit a Bug Report opens `https://github.com/nroduit/Weasis/issues` (`220-bug-report.png`). [help-browser-live.txt](help-browser-live.txt). |
| Presets / LUT import | Palette `134`, Preset `135`; **no file-chooser** — 25 files in `resources/luts/` ([lut-import.md](lut-import.md), [lut-files-sha256.txt](lut-files-sha256.txt)) and `presets.xml` |
| File → Export DICOM | Submenu `139-file-export.png`. Live wizard `177` Local Device, `178` Image Export Options, `179` DICOM Send, `180` CD/DVD Image. Transcoding combo includes JPEG XL `.110`/`.111`/`.112`, not HTJ2K. [export-dicom-dialog.md](export-dicom-dialog.md) |
| File → Print | Submenu `137-file-print.png`. Live `DicomPrintDialog` `187-dicom-print.png` (empty printer list; film defaults BLUE FILM / 8INX10IN / DPI 100). Live `PrintDialog` `188-print-2d-layout.png` (Centralized, DPI 150, annotations on; selected-view checkbox omitted on 1-cell layout). [print-dialogs.md](print-dialogs.md) |
| GSPS graphic types | Bytecode list [gsps-graphic-types.md](../gsps-graphic-types.md): POINT/POLYLINE/MULTILINE/INTERPOLATED/CIRCLE/ELLIPSE/MULTIPOINT/RECTANGLE/RULER/ARROW rendered; INFINITELINE/CUTLINE/RANGELINE/AXIS/CROSSHAIR not. Corpus `G-P0-iod-pr-gsps-types.dcm`. |

## Signed §2 add (`G-P0-019`)

HTJ2K `.201`/`.202`/`.203` decode/open — signed in [g-p0-019-reviewer.md](g-p0-019-reviewer.md). Not export, not 16-bit `BE`.

## Signed census-absent (`G-P0-019`)

`cdb-ext` — signed strike in [g-p0-019-reviewer.md](g-p0-019-reviewer.md). Not a 4.7 `ConfigData` token.
