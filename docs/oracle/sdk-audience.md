# SDK audience (G-P0-062)

Weasis in-house OSGi plugins will not load in Go. v1 = compile-in only.
WASM/RPC plugin SDK is later. Phase 4 still needs RPC-serializable command-list / layout-model types (`O-P4-018`).
