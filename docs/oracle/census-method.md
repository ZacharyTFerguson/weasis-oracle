# Census method (G-P0-014)

Contract of record is the **4.7 binary**, not README on master.

## How to run

1. Install Weasis **v4.7.0** native distro (`docs/oracle/reference.json`).
2. Enumerate `bundle/` — record every JAR; **open one object per claimed transfer syntax**.
3. Screenshot every menu, context menu, toolbar, prefs page.
4. Capture console `help`, installer OS minima, file associations.
5. Dump `weasis.properties` keys (shipped 114 + runtime overlay [census/weasis-properties-runtime.txt](census/weasis-properties-runtime.txt)), launcher args, about/help, LUT import.
6. **Symmetric:** README-absent behavior the binary does is **added** to checklist §2 (amendment). README rows the binary lacks are struck only with evidence + README quote via `G-P0-019`.

## Status

Bundle SHA inventory, Debian installer metadata, LUT list, and **running-binary screenshots** are in `docs/oracle/census/` (`binary-run.md`), including 2D Viewer Filter/Zoom/Orientation/Cine/Sort/Reset/Open-in-new-tab flyouts (`143`–`149`), nested image-context flyouts (`164`–`170`), and unique-UID GUI opens for JPEG-LS/lossless (`150`–`151`), RLE/Deflate/JPEG-extended (`156`–`158`). Live gogo `help` is in [gogo-help-live.txt](census/gogo-help-live.txt) (default GUI still does not bind 17179). Windows installer floor is **Vista+** from the shipped x64 MSI LaunchCondition `VersionNT >= 600` ([census/windows-msi-launchcondition.txt](census/windows-msi-launchcondition.txt)), not Windows 10; Weasis source WiX is undeclared ([census/windows-installer.md](census/windows-installer.md)). Linux arm64 `.deb` control matches amd64 Depends. GitHub `v4.7.0` asset names in [census/github-v4.7.0-assets.md](census/github-v4.7.0-assets.md). `G-P0-019` sign-off is **not** done.

## HTJ2K / XML graphics (G-P0-016 / 017)

`G-P0-016` is recorded in `docs/oracle/htj2k-probe.md`: 4.7 OpenCV native **decodes** HTJ2K (`.201`/`.202`/`.203`); export `TransferSyntax` enum does not list it. GUI open screenshots still belong under `G-P0-014`. XML graphics (`G-P0-017`) is still a binary/census feed, not a README guess.
