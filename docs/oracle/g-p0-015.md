# G-P0-015 vertical dry run — unsigned author notes

**Status:** author notes only. **Do not tick `G-P0-015` from this file.** Second-person review belongs in [`g-p0-015-reviewer.md`](g-p0-015-reviewer.md) (that file does not exist until the reviewer writes it). Checklist merge-last remains `[ ]`. Phase 1 is not started.

`G-P0-006` / `G-P0-014` / `G-P0-019` are already ticked from other signed files. This packet is only the sort-key + Felix-tree dry-run leaf.

## Required chain (plan / checklist)

1. Harness golden from calling Weasis — `SortKeyHarness` → `corpus/goldens/G-P0-015.json` (`genesis.oracle.v1`, class `ST`, sha256 `1883d029a3187452c1eae5e1c4bb308662a0149fb6194e28334101707aaa9483`, 1422 bytes). Dual-run `G-P0-015-run1.json` / `G-P0-015-run2.json` are byte-identical (`cmp`). Producer field `weasis-library`.
2. Mapping-guide entry — [`JAVA-TO-GO.md`](../../JAVA-TO-GO.md) six `SortSeriesStack.*` rows → `oracle.Cmp*`.
3. Go port — [`internal/oracle/sortkey.go`](../../internal/oracle/sortkey.go) (`cmpNilEither` copies 4.7 null-either-side `0`; `doubleCompare` copies `Double.compare` including NaN / `-0.0`; `CmpLocalTime` is clock-only like `LocalTime.compareTo`). Bytecode: [`sortseriesstack-bytecode.md`](sortseriesstack-bytecode.md).
4. Oracle test bound to case id — `TestSortKeyGolden_matchesGoLeaf` in `internal/oracle/sortkey_test.go`.
5. Second structural dry run (Felix-boot from `G-P0-000`) — `TestFelix_dicomModelTreeDryRun` consumes `corpus/goldens/G-P0-000-felix.json` (OpeningViewer `NONE`/`ALL_PATIENTS`; explorer mimeTypes `series/dicom` `pr/dicom` `ko/dicom` `seg/dicom` `sr/dicom`; HSM `KOSpecialElement` / `PRSpecialElement` / `SegSpecialElement`).
6. Adversarial review — **not this file**.
7. §5 block — unsigned paste in [`gate-0a-signoff-draft.md`](gate-0a-signoff-draft.md).
8. Merge last via draft PR — not until Pass + §5.

Inverse Stack on the 2D Viewer menu is a reverse flag, not a seventh comparator. `SortSeriesStack.getValues()` English order: Instance Number, Anatomical Direction (`TagW.SlicePosition`), Slice Location, Content Time, Acquisition Time, Diffusion b-value.

## Independently re-executed on this coordinator (not a Pass)

Parent HEAD was `2e3fddf` then packet `2647d0e`. Local after bytecode port:

- `go test ./internal/oracle` — **ok** (includes `TestDoubleCompare_matchesJava`, `TestCmpLocalTime_clockOnly`)
- `go run ./cmd/weasis-dump dump sort-key` — six comparators, ten pairs, signs match the golden
- `SortKeyHarness` recompiled against pinned `oracle/java/lib` jars and rewritten to `/tmp` — **sortKey / schema / reference match** committed `G-P0-015.json` (platform Linux amd64 JDK `25.0.4.1+1-LTS`)
- `javap` of `SortSeriesStack$1`–`$6` recorded in [`sortseriesstack-bytecode.md`](sortseriesstack-bytecode.md)

NOTICE now names the sort-key comparators as Apache-2.0 derived leaf algorithms.

## What this is not

- Not Phase 1 identity/ingest/`dump` implementation
- Not a Spike A–D throwaway tree
- Not a census tick (014/019 already signed elsewhere)
- Not a substitute for `g-p0-015-reviewer.md`

Signer: ____ (not the author)
